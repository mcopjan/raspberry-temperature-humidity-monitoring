<UserControl x:Class="Raspberry.Temperature.Humidity.WPF.Desktop.Client.Views.ConfigurationNotificationView"
             x:Name="ConfigView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Raspberry.Temperature.Humidity.WPF.Desktop.Client.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Text="Api Url:" VerticalAlignment="Center" Margin="10,0,0,0"/>
        <TextBox Grid.Row="0" Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Center" Text="{Binding ConfigUrl}" Margin="20,0,20,0"/>
        <Button  Grid.Row="1" Grid.Column="1" Content="Save" Height="30" Width="60" HorizontalAlignment="Right" Margin="0,20,20,20" Command="{Binding ClickSaveCommand}" CommandParameter="{Binding ElementName=ConfigView}"/>
    </Grid>
</UserControl>
